# dynamic/dashboard.yml   ← 完整正确版，直接替换
http:
  routers:
    dashboard:
      rule: Host(`traefik.grep2000.com`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard/`))  # ← 这里必须 /dashboard/
      service: api@internal
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      middlewares:
        - auth-dashboard
      priority: 1000   # 可选：加个高优先级，防止被其他路由抢

  middlewares:
    auth-dashboard:
      basicAuth:
        users:
          - "admin:$apr1$swPBZ14p$G/8g3SZn90lIeHYqVIanm1"   # 你原来的密码
