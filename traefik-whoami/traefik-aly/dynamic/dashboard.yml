
# dynamic/dashboard.yml   ← 完整正确版，直接替换
http:
  routers:
    dashboard:
<<<<<<< HEAD
      rule: Host(`traefik-aly.165.22.241.196.nip.io`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard/`))  
      # rule: Host(`traefik-aly.grep2000.com`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard/`)) 
=======
      rule: Host(`traefik-aly.grep2000.com`) && (PathPrefix(`/api`) || PathPrefix(`/dashboard`))  # ← 这里必须 /dashboard/
>>>>>>> 91dc76f9a6c728b9165365d5924a57132e7d1356
      service: api@internal
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt
      middlewares:
        - auth-dashboard
      priority: 1000   # 可选：加个高优先级，防止被其他路由抢

  middlewares:
    auth-dashboard:
      basicAuth:
        users:
          - "admin:$apr1$swPBZ14p$G/8g3SZn90lIeHYqVIanm1"   # 你原来的密码
