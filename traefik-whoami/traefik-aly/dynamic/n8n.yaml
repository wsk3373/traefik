## n8n.yaml
http:
  routers:
    headscale:
      # rule: Host(`n8n.47.119.112.92.nip.io`)
      rule: Host(`n8n.grep2000.com`)
      service: n8n-vps
      entryPoints:
        - websecure
      tls:
        certResolver: letsencrypt

  services:
    n8n-vps:
      loadBalancer:
        servers: http://n8n:5678
